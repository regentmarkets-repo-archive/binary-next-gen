import expect from 'expect';
import { shallow } from 'enzyme';

describe('AppStateProvider', () => {
    it('should not swallow any error', () => {

    });

    it('should show loadingview before ws try auth returns', () => {

    });

    it('should show children after ws try auth returns, whether error or not', () => {

    });
});

