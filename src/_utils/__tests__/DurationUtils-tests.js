import * as du from '../DurationUtils';
import { expect } from 'chai';

/**
 *  Duration refer to string with digit follow by duration unit
 *  eg. '5h', '2t' ...
 */

describe('isDurationWithinRange', () => {
    it('should return true if duration is within range', () => {
        
    })
});