export default ({ theme }) => ({
    show: true,
    realtime: true,
    start: 20,
    end: 50,
    height: 20,
    backgroundColor: theme.background,
    dataBackgroundColor: theme.text,
    fillerColor: theme.fill,
    handleColor: theme.line,
    handleSize: 10,
    showDetail: false,
});
